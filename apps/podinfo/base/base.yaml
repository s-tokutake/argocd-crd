apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: podinfo
spec:
  source:
    path: manifests
    repoURL: 'https://github.com/s-tokutake/argocd-demo.git'
  syncPolicy:
    automated:
      automated:
        prune: false
        selfHeal: false
      prune: true
